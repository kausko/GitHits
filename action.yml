name: 'GitHits-Action'
description: 'Permanent storage for GitHub Traffic Insights'
branding:
  icon: 'pocket'
  color: 'blue'
inputs:
  pat:
    description: 'GitHub Personal Access Token'
    required: true
outputs:
  status:
    description: 'Output message of GitHits Action'
runs:
  using: 'node12'
  main: 'dist/index.js'